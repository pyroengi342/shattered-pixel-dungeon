import type { StandardJSONSchemaV1 as BaseStandardJSONSchemaV1, StandardSchemaV1 as BaseStandardSchemaV1, StandardTypedV1 } from '@standard-schema/spec';
import type { InputSchema } from './common.js';
/**
 * Re-export of the official Standard Schema spec types.
 */
export type { BaseStandardJSONSchemaV1 as StandardJSONSchemaV1, BaseStandardSchemaV1 as StandardSchemaV1, StandardTypedV1, };
/**
 * Standard Schema validator constrained to object-like tool args.
 */
export type StandardInputValidatorSchema = BaseStandardSchemaV1<Record<string, unknown>, Record<string, unknown>>;
/**
 * Standard JSON Schema converter constrained to object-like tool args.
 */
export type StandardInputJsonSchema = BaseStandardJSONSchemaV1<Record<string, unknown>, Record<string, unknown>>;
/**
 * Schema that supports both Standard Schema validation and JSON Schema conversion.
 */
export type StandardInputSchemaWithJson = StandardInputValidatorSchema & StandardInputJsonSchema;
/**
 * Any input schema accepted by tool descriptors.
 *
 * Runtime adapters may normalize all inputs to a Standard Schema validator.
 * When a schema implements both Standard Schema validation and Standard JSON
 * Schema conversion, adapters may prefer JSON conversion to keep validation
 * errors consistent across plain JSON Schema and Standard-compatible inputs.
 */
export type ToolInputSchema = InputSchema | StandardInputValidatorSchema | StandardInputJsonSchema;
/**
 * Any output schema accepted by tool descriptors.
 */
export type ToolOutputSchema = InputSchema | StandardInputJsonSchema;
//# sourceMappingURL=standard-schema.d.ts.map